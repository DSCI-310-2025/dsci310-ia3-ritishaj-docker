services:
  analysis-env:
    image: ritishaj/docker-practice
    ports:
      - "8787:8787"
    volumes:
      - .:/home/rstudio/ia3
    environment:
      PASSWORD: "password"
      DISABLE_AUTH: true